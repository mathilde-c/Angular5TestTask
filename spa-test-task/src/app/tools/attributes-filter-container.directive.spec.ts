import { AttributesFilterContainerDirective } from "./attributes-filter-container.directive";

describe("AttributesFilterContainerDirective", () => {
  it("should create an instance", () => {
    const directive = new AttributesFilterContainerDirective(null);
    expect(directive).toBeTruthy();
  });
});
